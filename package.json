{
  "name": "arena-backend",
  "version": "1.0.0",
  "description": "Backend for multiple projects",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node -e \"require('dotenv'); require('express'); require('socket.io'); console.log('deps ok')\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "rate-limiter-flexible": "^2.4.1",
    "socket.io": "^4.7.2"
  }
}
